# Phase 0.1 — File-to-Project Mapping

> Every file in the current monorepo mapped to its destination.
> Three target projects: **Golf Ball**, **Hub**, **Both** (duplicated into each).
> Completed: April 25, 2026.
>
> Do not begin Phase 1 until this table is reviewed and confirmed.

---

## Destination key

| Label | Meaning |
|---|---|
| **Golf Ball** | Lives only in `findmyidealgolfball.com` project |
| **Hub** | Lives only in `findmyideal.info` project |
| **Both** | Duplicated into both projects (and every future product clone) |
| **DELETE** | Platform abstraction — no longer needed in single-product projects |
| **Notes** | Any rename, simplification, or action required |

---

## Root config files

| File | Golf Ball | Hub | Action / Notes |
|---|---|---|---|
| `package.json` | ✓ | ✓ | Both — adapt name and scripts per project |
| `package-lock.json` | ✓ | ✓ | Both — regenerated on install |
| `tsconfig.json` | ✓ | ✓ | Both — identical |
| `next.config.ts` | ✓ | ✓ | Both — currently empty, identical |
| `next-env.d.ts` | ✓ | ✓ | Both — auto-generated by Next.js |
| `postcss.config.mjs` | ✓ | ✓ | Both — identical |
| `eslint.config.mjs` | ✓ | ✓ | Both — identical |
| `jest.config.js` | ✓ | — | Golf Ball only — hub has no tests |
| `.env.example` | ✓ | ✓ | Both — adapt variable names per project |
| `.env.local` | ✓ | ✓ | Both — different values per project (ADMIN_PASSWORD, GA4 ID, SITE_URL) |
| `.gitignore` | ✓ | ✓ | Both — identical |
| `README.md` | ✓ | ✓ | Both — rewrite per project |
| `proxy.ts` | **DELETE** | **DELETE** | Domain-routing middleware — not needed in single-product projects |
| `NEXT-STEPS.md` | — | — | **DELETE** — superseded by this split plan |
| `PILLOW-MVP-PRESTART.md` | — | — | Keep as planning doc outside repos, or archive |

---

## `.github/` and `.husky/`

| File | Golf Ball | Hub | Action / Notes |
|---|---|---|---|
| `.github/workflows/ci.yml` | ✓ | ✓ | Both — identical CI pipeline |
| `.husky/pre-push` | ✓ | ✓ | Both — identical |
| `.vscode/settings.json` | ✓ | ✓ | Both — developer convenience, identical |

---

## `app/` — root layout and providers

| File | Golf Ball | Hub | Action / Notes |
|---|---|---|---|
| `app/layout.tsx` | ✓ | ✓ | Both — remove host-header routing in Golf Ball; hub version is simpler (no admin cookie, no GA4 quiz events) |
| `app/globals.css` | ✓ | ✓ | Both — identical |
| `app/favicon.ico` | ✓ | ✓ | Both — replace with product-specific icon per project |
| `app/client-providers.tsx` | ✓ | — | Golf Ball only — answers state, questionnaire version state; hub has no quiz |
| `app/client-root.tsx` | ✓ | — | Golf Ball only — wraps Providers + ErrorBoundary |
| `app/client-wrapper.tsx` | ✓ | — | Golf Ball only — thin Providers wrapper (consider consolidating with client-root) |
| `app/providers.tsx` | **DELETE** | **DELETE** | Empty file — already deprecated |
| `app/(with-providers)/layout.tsx` | ✓ | — | Golf Ball only — quiz pages need Providers; hub has no quiz |

---

## `app/` — pages

| File | Golf Ball | Hub | Action / Notes |
|---|---|---|---|
| `app/page.tsx` | ✓ | ✓ | **Split** — golf ball homepage (product CTA) goes to Golf Ball; hub listing page (isHub branch) goes to Hub as its own `app/page.tsx` |
| `app/about/page.tsx` | — | ✓ | **Hub only** — brand-level content, references findmyideal.info; update copy to remove golf-specific mention |
| `app/privacy-policy/page.tsx` | — | ✓ | **Hub only** — brand-level, references findmyideal.info |

---

## `app/` — golf ball routes (direct, non-dynamic)

| File | Golf Ball | Hub | Action / Notes |
|---|---|---|---|
| `app/golf-ball/questionnaire/page.tsx` | ✓ | — | Golf Ball — rename to `app/questionnaire/page.tsx` (no category prefix needed in single-product project) |
| `app/golf-ball/results/page.tsx` | ✓ | — | Golf Ball — rename to `app/results/page.tsx` |
| `app/golf-ball/[slug]/GolfBallSeoLandingPage.tsx` | ✓ | — | Golf Ball — rename to `app/[slug]/SeoLandingPage.tsx` |
| `app/golf-ball/[slug]/page.tsx` | ✓ | — | Golf Ball — rename to `app/[slug]/page.tsx` |

---

## `app/` — dynamic category routes (platform abstraction)

| File | Golf Ball | Hub | Action / Notes |
|---|---|---|---|
| `app/[category]/questionnaire/page.tsx` | **DELETE** | **DELETE** | Platform abstraction — direct routes replace this |
| `app/[category]/questionnaire/layout.tsx` | **DELETE** | **DELETE** | Platform abstraction |
| `app/[category]/results/page.tsx` | **DELETE** | **DELETE** | Platform abstraction — direct routes replace this |
| `app/[category]/results/layout.tsx` | **DELETE** | **DELETE** | Platform abstraction |

---

## `app/` — legacy redirect routes

| File | Golf Ball | Hub | Action / Notes |
|---|---|---|---|
| `app/questionnaire/page.tsx` | **DELETE** | **DELETE** | Redirects to `/golf-ball/questionnaire` — no longer needed once routes are renamed |
| `app/questionnaire/v2/page.tsx` | **DELETE** | **DELETE** | Re-exports v2-page — legacy only |
| `app/questionnaire/v2-page.tsx` | ✓ | — | Golf Ball — the actual questionnaire UI; rename to `app/questionnaire/page.tsx` (consolidate) |
| `app/results/page.tsx` | ✓ | — | Golf Ball — the actual results UI; becomes `app/results/page.tsx` directly. **Remove golf-ball fallback** |

> **Check analytics before deleting legacy redirects** — confirm `/questionnaire` and `/questionnaire/v2` are not receiving live traffic from external links or search results.

---

## `app/` — admin

| File | Golf Ball | Hub | Action / Notes |
|---|---|---|---|
| `app/admin/golf-ball/page.tsx` | ✓ | — | Golf Ball — rename to `app/admin/page.tsx` (no category prefix needed) |
| `app/admin/page.tsx` | ✓ | — | Golf Ball — currently just `export { default } from "./theme-admin"` — merge or keep as redirect |
| `app/admin/theme-admin.tsx` | ✓ | — | Golf Ball — keep as-is |
| `app/admin/login/page.tsx` | ✓ | — | Golf Ball — keep as-is |

---

## `components/`

All UI primitives. Duplicated into both projects. Hub uses a subset.

| File | Golf Ball | Hub | Action / Notes |
|---|---|---|---|
| `components/Header.tsx` | ✓ | ✓ | Both — simplify Hub version (no categoryRegistry dependency) |
| `components/Footer.tsx` | ✓ | ✓ | Both |
| `components/Button.tsx` | ✓ | ✓ | Both |
| `components/Badge.tsx` | ✓ | ✓ | Both |
| `components/Card.tsx` | ✓ | ✓ | Both |
| `components/ErrorBoundary.tsx` | ✓ | ✓ | Both |
| `components/ResultsCard.tsx` | ✓ | — | Golf Ball only — hub has no results |
| `components/Hero.tsx` | ✓ | ✓ | Both — hub may use for landing |
| `components/Progress.tsx` | ✓ | — | Golf Ball only — questionnaire progress bar |
| `components/StepContainer.tsx` | ✓ | — | Golf Ball only — questionnaire step wrapper |

---

## `config/`

| File | Golf Ball | Hub | Action / Notes |
|---|---|---|---|
| `config/golf-ball/questionnaire.v1.ts` | ✓ | — | Golf Ball — rename to `config/product/questionnaire.v1.ts` |
| `config/golf-ball/questionnaire.v2.ts` | ✓ | — | Golf Ball — rename to `config/product/questionnaire.v2.ts` |
| `config/golf-ball/questionnaire.v3.ts` | ✓ | — | Golf Ball — rename to `config/product/questionnaire.v3.ts` |
| `config/golf-ball/questionnaire.versions.ts` | ✓ | — | Golf Ball — rename to `config/product/questionnaire.versions.ts` |
| `config/golf-ball/scoring.ts` | ✓ | — | Golf Ball — rename to `config/product/scoring.ts` |
| `config/golf-ball/scoring-v3.ts` | ✓ | — | Golf Ball — rename to `config/product/scoring-v3.ts` |
| `config/golf-ball/products.ts` | ✓ | — | Golf Ball — rename to `config/product/products.ts` |
| `config/golf-ball/seo-pages.ts` | ✓ | — | Golf Ball — rename to `config/product/seo-pages.ts` |
| `config/questionnaire-config.json` | ✓ | — | Golf Ball — version store; keep as-is |
| `config/global-theme.json` | ✓ | ✓ | Both — each project has its own active theme |
| `config/registry.ts` | **DELETE** | **DELETE** | Platform abstraction — no longer needed |
| `config/domain-map.ts` | **DELETE** | **DELETE** | Platform abstraction — no longer needed |

---

## `core/`

| File | Golf Ball | Hub | Action / Notes |
|---|---|---|---|
| `core/theme/index.ts` | ✓ | ✓ | Both |
| `core/theme/ThemeProvider.tsx` | ✓ | ✓ | Both |
| `core/theme/themes.ts` | ✓ | ✓ | Both |
| `core/theme/tokens.ts` | ✓ | ✓ | Both |
| `core/types/index.ts` | ✓ | — | Golf Ball only — Product, Questionnaire, ScoringEngine; hub has no products |
| `core/category-context.tsx` | **DELETE** | **DELETE** | Platform abstraction — `categoryId` context not needed in single-product project |
| `core/state.tsx` | **DELETE** | **DELETE** | Already deprecated (file contains only a deprecation comment) |

---

## `lib/`

| File | Golf Ball | Hub | Action / Notes |
|---|---|---|---|
| `lib/analytics.ts` | ✓ | — | Golf Ball only — quiz event tracking; hub has no quiz |
| `lib/scoring.ts` | ✓ | — | Golf Ball only — ScoringEngine type definition |
| `lib/scoring-v2.ts` | **DELETE** | **DELETE** | Re-export shim only (`export { scoreGolfBall as scoreProductV2 }`) — delete and update any imports |
| `lib/scoring-v2.test.ts` | ✓ | — | Golf Ball — rename to `lib/scoring.test.ts` |

---

## `pages/api/`

| File | Golf Ball | Hub | Action / Notes |
|---|---|---|---|
| `pages/api/admin-login.ts` | ✓ | — | Golf Ball only — hub has no admin |
| `pages/api/admin-logout.ts` | ✓ | — | Golf Ball only |
| `pages/api/set-questionnaire.ts` | ✓ | — | Golf Ball only — remove `VALID_CATEGORIES` registry dependency; import product versions directly |
| `pages/api/set-theme.ts` | ✓ | — | Golf Ball only |

---

## `public/`

| File | Golf Ball | Hub | Action / Notes |
|---|---|---|---|
| `public/images/*.png` (all golf ball product images) | ✓ | — | Golf Ball only |
| `public/file.svg` | ✓ | ✓ | Both — Next.js boilerplate, can delete if unused |
| `public/globe.svg` | ✓ | ✓ | Both — Next.js boilerplate, can delete if unused |
| `public/next.svg` | ✓ | ✓ | Both — Next.js boilerplate, can delete if unused |
| `public/vercel.svg` | ✓ | ✓ | Both — Next.js boilerplate, can delete if unused |
| `public/window.svg` | ✓ | ✓ | Both — Next.js boilerplate, can delete if unused |

---

## `tests/`

| File | Golf Ball | Hub | Action / Notes |
|---|---|---|---|
| `tests/e2e-smoke.spec.ts` | ✓ | — | Golf Ball — update assertions after route renames |
| `tests/e2e-v2.spec.ts` | ✓ | — | Golf Ball — update URL assertions after route renames |

---

## Summary counts

| Destination | File count |
|---|---|
| Golf Ball only | 42 |
| Hub only | 7 |
| Both (duplicated) | 27 |
| DELETE | 16 |

---

## Files requiring action before Phase 1 can begin

These are not renames — they require code changes:

| File | Required change |
|---|---|
| `app/results/page.tsx` | Remove `categoryRegistry['golf-ball']` fallback — import product config directly |
| `app/layout.tsx` | Remove host-header routing; import product config directly |
| `app/golf-ball/[slug]/GolfBallSeoLandingPage.tsx` | Remove hardcoded `/golf-ball/questionnaire` href — update to `/questionnaire` |
| `pages/api/set-questionnaire.ts` | Remove registry dependency; import product versions directly |
| `app/questionnaire/v2-page.tsx` | Remove `questionnaireV2` default import (currently a fallback) — always receive questionnaire as prop |
| `components/Header.tsx` | Remove `categoryRegistry` import — read `brandName` from a direct config import |
| `tests/e2e-smoke.spec.ts` | Update route and assertion strings after renames |
| `tests/e2e-v2.spec.ts` | Update URL assertions after renames |

---

## Pre-Phase 1 confirmation checklist

- [x] This mapping table has been reviewed and agreed
- [ ] **Analytics check required:** `/questionnaire` and `/questionnaire/v2` legacy routes are referenced only in the E2E test suite (no internal hrefs in production UI). Check GA4 to confirm no significant external traffic before deleting. Likely safe — `app/questionnaire/page.tsx` is a redirect to `/golf-ball/questionnaire` and has been since refactor.
- [ ] Vercel account confirmed: able to create separate projects for Golf Ball and Hub
- [ ] GA4 plan confirmed: keep `G-B9MGT1T8NV` for Golf Ball; new stream/property for Hub
- [x] **Build baseline confirmed: CLEAN** — `npm run build` passed with zero errors (April 25, 2026). All routes compiled successfully.

## Build baseline — routes confirmed live

App routes confirmed by build output:
- `/` (homepage — hub/golf-ball dual mode)
- `/[category]/questionnaire` (dynamic — to be deleted)
- `/[category]/results` (dynamic — to be deleted)
- `/about`
- `/admin`
- `/admin/golf-ball`
- `/admin/login`
- `/golf-ball/[slug]` (SEO landing pages)
- `/golf-ball/questionnaire`
- `/golf-ball/results`
- `/privacy-policy`
- `/questionnaire` (legacy redirect — safe to delete after analytics check)
- `/questionnaire/v2` (legacy — safe to delete after analytics check)
- `/results` (legacy — safe to delete after analytics check)

API routes confirmed:
- `/api/admin-login`
- `/api/admin-logout`
- `/api/set-questionnaire`
- `/api/set-theme`
