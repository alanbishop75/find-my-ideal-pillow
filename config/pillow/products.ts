/**
 * config/pillow/products.ts
 *
 * STUB — product data pending sourcing and attribute verification.
 * Replace with real products before launch (Phase 5).
 */
import type { Product } from "../../core/types";

export const products: Product[] = [];
